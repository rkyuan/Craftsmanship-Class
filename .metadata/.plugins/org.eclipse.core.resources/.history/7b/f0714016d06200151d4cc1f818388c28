package orange;

import static org.junit.Assert.*;

import org.junit.Test;

public class requestTest {
	@Test
	public void test() {
		Exchange.Builder b = new Exchange.Builder();
		b.addCompatible(new SerialNumber(235));
		Exchange e = b.build();
		System.out.println(e.getCompatibleProducts());
		
	}

}
